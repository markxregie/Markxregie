<div align="center">

```
┌─────────────────────────────────────────────┐
│  guest@portfolio:~$ whoami                   │
│  mark_regie_a._magtangob                     │
│                                               │
│  role   : frontend developer                 │
│  goal   : network engineer (in progress)     │
│  status : available // building              │
└─────────────────────────────────────────────┘
```

</div>

<br>

## `~/about`

Frontend developer working primarily in React and Next.js, building toward a parallel path in network engineering. I care about clean architecture, predictable systems, and understanding what's happening below the UI layer — not just above it.

Currently splitting time between shipping frontend work and studying networking fundamentals (routing, switching, subnetting).

---

## `~/stack`

```
frontend/
├── html
├── css
├── javascript
├── react
└── next.js

backend/
├── node.js
├── c#
└── .net

data/
├── sqlite
└── mysql

tooling/
├── docker
├── git / github
├── vscode
└── linux
```

| category   | tools                              |
|------------|--------------------------------------|
| frontend   | `HTML` `CSS` `JS` `React` `Next.js`  |
| backend    | `Node.js` `C#` `.NET`                |
| database   | `SQLite` `MySQL`                     |
| tooling    | `Docker` `Git` `Linux` `VS Code`     |

---

## `~/projects`

```
$ ls -la featured/
```

**`project-one/`**
Short technical scope of the project — what it does, in one line.
`stack: react · next.js · node.js`

**`project-two/`**
Short technical scope of the project — what it does, in one line.
`stack: c# · .net · mysql`

**`project-three/`**
Short technical scope of the project — what it does, in one line.
`stack: javascript · sqlite · docker`

> Replace the three entries above with your actual pinned repos — name, one-line scope, and stack.

---

## `~/log`

```
$ tail -f current_focus.log
```

- building and refining frontend projects with React / Next.js
- studying core networking concepts — subnetting, routing, switching
- working toward a relevant networking certification
- maintaining and cleaning up older repositories

---

## `~/connect`

```
$ cat contact.txt

github    :: github.com/markxregie
linkedin  :: linkedin.com/in/mark-regie-magtangob-a7b120202
portfolio :: markregiemagtangob.vercel.app
email     :: marklegend029@gmail.com
```

<div align="center">

```
user@portfolio:~$ _
```

</div>
